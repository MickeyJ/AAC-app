require('dotenv').config();
const express = require('express');
const router = express.Router();
const db = require('../db');

router.post('/category', (req, res, next) => {

});

router.post('/phrase', (req, res, next) =>{

});

module.exports = router;
